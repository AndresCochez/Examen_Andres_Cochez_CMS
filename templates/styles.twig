{# Definieer CSS variabelen voor kleurinstellingen #}
<style>
    :root {
        --primary-color: {{ colorSettings.primaryColor }};
        --secondary-color: {{ colorSettings.secondaryColor }};
        --background-color: {{ colorSettings.backgroundColor }};
        --text-color: {{ colorSettings.textColor }};
        --header-text-color: {{ colorSettings.headerTextColor }};
        --accent-color: {{ colorSettings.accentColor }};
        --price-label-color: {{ colorSettings.priceLabelColor }};
    }
</style>